if [ -n "$COLORTERM" -a "${TERM%-256color}" = "$TERM" ]
    then TERM=$TERM-256color
fi

if [ -f "$HOME/.aliasrc" ]
    then source $HOME/.aliasrc
fi

if [ -d "$HOME/.common.d" ]
    then source $HOME/.common.d/*
fi

if [ -f "$HOME/.localrc" ]
    then source $HOME/.localrc
fi
